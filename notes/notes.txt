//Unused or discarded features
#include <iostream>
#include <limits>

void waitForKey() {
    std::cout << "Press any key to continue....";
    // Clear any leftover characters (like '\n') in the input buffer
    std::cin.ignore(std::numeric_limits<std::streamsize>::max(), '\n');
    // Wait for the user to press Enter
    std::cin.get();
}